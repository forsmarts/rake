// Object which represents one cell of a puzzle

var cRakeCell = function(column, row, number){
  this.column = column;
  this.row = row;
  // defalt number in the cell
  this.number = number;
}
